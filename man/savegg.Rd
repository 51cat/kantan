% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/savegg.R
\name{savegg}
\alias{savegg}
\title{Save ggplot Object to File}
\usage{
savegg(
  plot = NULL,
  filename,
  width = 10,
  height = 8,
  dpi = 300,
  format = NULL,
  scale = 1,
  limitsize = FALSE,
  bg = "white",
  device = NULL,
  path = NULL,
  create_dir = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{plot}{A ggplot object. If NULL, saves the last plot.}

\item{filename}{Character. Output filename with extension.}

\item{width}{Numeric. Width in inches. Default is 10.}

\item{height}{Numeric. Height in inches. Default is 8.}

\item{dpi}{Numeric. Resolution in dots per inch. Default is 300.}

\item{format}{Character. Output format: "pdf", "png", "svg", "tiff", "jpeg", "eps", "ps".
If NULL, inferred from filename extension. Default is NULL.}

\item{scale}{Numeric. Scaling factor. Default is 1.}

\item{limitsize}{Logical. Whether to limit image size to 50x50 inches. Default is FALSE.}

\item{bg}{Character. Background color. Default is "white".}

\item{device}{Graphics device function. If NULL, inferred from format. Default is NULL.}

\item{path}{Character. Directory path to save file. If NULL, uses current directory. Default is NULL.}

\item{create_dir}{Logical. Whether to create directory if it doesn't exist. Default is TRUE.}

\item{verbose}{Logical. Whether to print save message. Default is TRUE.}
}
\value{
Invisibly returns the full path to the saved file.
}
\description{
Save ggplot Object to File
}
\examples{
\dontrun{
p <- ggplot2::ggplot(mtcars, ggplot2::aes(mpg, wt)) + ggplot2::geom_point()
savegg(p, "plot.png")
savegg(p, "output/plot.pdf", width = 12, height = 6)
savegg(p, "plot.png", dpi = 600, scale = 1.5)
savegg(filename = "last_plot.png")  # saves last plot
}
}
